@model IEnumerable<ResumeApp.Models.ProfSummary>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.applicantID)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ProfSum)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.applicantID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProfSum)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.profSummaryID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.profSummaryID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.profSummaryID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
